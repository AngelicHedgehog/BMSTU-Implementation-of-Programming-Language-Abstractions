/**
  <BinOp "+" | "-" | "*" | "/" | "%" | "&" | "|" | "~" | "<<" | ">>"> == s.AsmCodeCmd
*/
$ENTRY BinOp {
  "+" = ADD;
  "-" = SUB;
  "*" = MUL;
  "/" = DIV;
  "%" = MOD;
  "&" = BITAND;
  "|" = BITOR;
  "~" = BITNOT;
  "<<" = LSHIFT;
  ">>" = RSHIFT;
}
