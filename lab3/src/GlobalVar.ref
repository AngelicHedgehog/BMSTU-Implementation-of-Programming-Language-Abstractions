*$FROM src/Name.ref
*$FROM src/ConstExpr.ref
*$FROM src/Init.ref
$EXTERN Name, ConstExpr, Init;

/**
  <GlobalVar (var s.Name t.ConstExpr e.Init?)> == s.AsmCodeCmd*
*/
$ENTRY GlobalVar {
  (var s.Name t.ConstExpr) = /* пусто */;
  (var s.Name t.ConstExpr e.Init) = /* пусто */;
}
